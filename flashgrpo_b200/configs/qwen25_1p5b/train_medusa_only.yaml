inherits: train_hrdcr.yaml

method: medusa_only
run_name: qwen25_1p5b_medusa_only

# Fair proposal ablation: feedback, m_t, trust and sparse head updates remain on.
# Only the proposal-hidden injection is disabled.
reflex:
  enabled: true
  feedback_enabled: true
  proposal_injection_enabled: false

aux_update:
  enabled: true
